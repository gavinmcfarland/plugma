{
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "plugma dev",
		"build": "plugma build",
		"release": "plugma release"
	},
	"devDependencies": {
		"vite": "^5.4.10"<% if (type === 'plugin') { %>,
        "@figma/plugin-typings": "^1.100.2"
        <% } else { %>,
        "@figma/widget-typings": "^1.12.1"<% } %>,
		"plugma": "<%- versions.plugma %>"
	}
}
